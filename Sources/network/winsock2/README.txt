Win32 WinSock2 C-FFI definitions

Issue the command

  nmake -f make-sources

to generate the Dylan sources files.

Merge the file module.dylan that is created above into the file
win32-network-library.dylan in our parent directory.
