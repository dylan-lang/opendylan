===========================
JavaScript Compiler Backend
===========================

The Open Dylan compiler supports multiple backends, currently supporting
C, LLVM and the HARP native backend. In the past, it also had partial
support for the JVM.

This project would be to create a new backend that outputs JavaScript.