Network - Low Level API
***********************

The main API exposed by the network library is one using streams. We
expose both the low level POSIX API and the high level streams API,
with them clearly documented and exported as two separate modules.
